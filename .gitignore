# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android / IDE
local.properties
*.iml
.idea/
.kotlin/
captures/
.cxx/

# Signing / secrets — never commit release keys.
*.jks
*.keystore
keystore.properties
# ...except the throwaway DEBUG keystore, committed so CI builds share one key
# and sideloaded updates install cleanly.
!app/debug.keystore

# OS
.DS_Store

# Release signing material — never committed. Whoever holds this key can publish
# as this verified developer.
*.keystore
!app/debug.keystore
*.jks
keystore.properties

# Release runbook: kept locally, not in the repo (contains no secrets, but the
# release procedure is not something this repository needs to carry).
docs/RELEASING.md
