# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android / IDE
local.properties
*.iml
.idea/
.kotlin/
captures/
.cxx/

# Signing / secrets — never commit release keys.
*.jks
*.keystore
keystore.properties
# ...except the throwaway DEBUG keystore, committed so CI builds share one key
# and sideloaded updates install cleanly.
!app/debug.keystore

# OS
.DS_Store
